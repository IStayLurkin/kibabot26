services:
  searxng:
    image: searxng/searxng:latest
    container_name: kiba-searxng
    ports:
      - "8080:8080"
    volumes:
      - ./searxng_config:/etc/searxng:rw
    restart: unless-stopped
    environment:
      - SEARXNG_BASE_URL=http://localhost:8080
